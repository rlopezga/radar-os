# data

Espacio para persistencia local de `radar-os`.

Uso actual:

- `radar-os.sqlite` guarda el transcript bruto como evidencia local del proyecto
- no equivale a memoria canónica
- no sustituye la validación ni la integración posterior en `atenea`

Regla:

- el bruto se conserva aquí antes de cualquier interpretación externa
